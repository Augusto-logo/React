Landing page from https://www.frontendmentor.io/challenges/product-preview-card-component-GO7UmttRfa

This page was created with the objective to learn how to use Tailwind on React.

comments:

# Imported and used fonts from google fonts.

_The Tailwind docs have all the explanations on how to use_.

1. It's possible to import all the google fonts from the same URL.
2. The texts on **URL** are the optional configurations from the import. So, if you remove the `ital`, it will remove the italic from the **Font**. Same as the weights `wght@`.
3.

- Inserted Icon on a button
